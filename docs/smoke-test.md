# Smoke Test: roadmap-orchestrator

Дата: 2026-06-11
Тестовый проект: `/tmp/smoke-test-project` (ROADMAP.md с двумя пунктами)

---

## Чек-лист

### U1: Каркас и установка

- [x] `git init` — репозиторий инициализирован
- [x] `install.sh` идемпотентен (повторный запуск → код 0, симлинки не меняются)
- [x] Симлинк `~/.claude/skills/roadmap-orchestrator` → `skills/roadmap-orchestrator`
- [x] Симлинк `~/.agents/skills/roadmap-orchestrator` → `skills/roadmap-orchestrator`
- [x] `.gitignore` исключает `.env*` и `settings.json`
- [x] Скилл появился в списке доступных (`roadmap-orchestrator` виден в system-reminder)

### U2: SKILL.md — контракт и конвейер

- [x] Frontmatter: `name: roadmap-orchestrator`, `description` содержит триггерные фразы на русском и английском
- [x] Правило «не пишет сам» заявлено как жёсткое ограничение
- [x] Два исключения чётко определены: диалог с пользователем + учёт состояния
- [x] Приём роадмапа: поиск файла → выбор первого `- [ ]` → подтверждение
- [x] Resume: то же правило (первый незакрытый пункт)
- [x] Поэтапный поток: A (спека), B (план), C (реализация), D (ревью кода)
- [x] Каждый этап — гейт волны ревью
- [x] Внутренние per-task ревью superpowers не считаются волнами R4
- [x] Ссылки на все reference-файлы присутствуют

### U3: Протокол ревью и промпты

- [x] `review-protocol.md`: цикл волны, контракт находок, критерий остановки
- [x] Кап 5 волн задокументирован
- [x] Память волн: правило «закрытое не пере-поднимать»
- [x] Путь DISPUTED: немедленная эскалация
- [x] Ошибка транспорта: не считается «ноль находок», не расходует волну
- [x] Валидация вывода: тег серьёзности обязателен, prompt injection проверяется
- [x] Эскалация: формат задокументирован
- [x] `spec-review.md`: плейсхолдеры, BINDING_CONSTRAINTS, PREVIOUS_WAVE_FINDINGS
- [x] `plan-review.md`: аналогично
- [x] `code-review.md`: BASE_SHA/HEAD_SHA, совместим с superpowers:receiving-code-review
- [x] Нейтральность промптов: ручная вычитка — наводящих фраз нет (найденное "could be better" — правило классификации, не наводка)

### U4: Рецепты спавна

- [x] `spawn-recipes.md`: рецепты для Claude Code (Task/Agent) и `codex exec`
- [x] Пути к SKILL.md superpowers задокументированы
- [x] Проверки доступности: `command -v codex`, `command -v deepcode`
- [x] Правило честной деградации задокументировано
- [x] API-ключи: предупреждение против флагов CLI
- [x] `fallback-mode.md`: пре-флайт для Codex-оркестратора
- [x] DeepSeek вариант A (deepcode CLI): задокументирован, headless не проверен (ключ не настроен)
- [x] DeepSeek вариант B (API напрямую): задокументирован с рецептом curl
- [x] Follow-up DeepSeek smoke test: явно именован в fallback-mode.md
- [x] `~/.agents/skills/superpowers/` не резолвится — инструкция по созданию симлинка в fallback-mode.md

### U5: Рубрика маршрутизации

- [x] Три яруса: механический / интеграционный / архитектурный
- [x] Таблица текущего соответствия помечена как редактируемая
- [x] Правило неоднозначности: маршрутизировать вверх, никогда вниз
- [x] Re-dispatch при BLOCKED задокументирован
- [x] Кросс-модельная матрица (кто кого ревьюит) присутствует

### U6: Дымовой прогон

- [x] `codex exec -s read-only` — работает, не модифицирует репозиторий (PASS)
- [x] Нейтральность промптов — ручная вычитка (PASS)
- [x] Codex возвращает структурированные находки по контракту — **PASS**

---

## Проверка `codex exec` с реальным промптом ревью спеки

Тестовая спека: `/tmp/test-spec.md` (Todo CLI, 3 требования)

### Результат

Команда: `codex exec -s read-only < /tmp/review-prompt.txt`

```
### Находки

#### Critical (Must Fix)
- Не определена модель выполнения CLI: при "в памяти без персистентности"
  сценарий add→list в новом запуске теряет задачу, R1/R2/R3 неоднозначны.

#### Important (Should Fix)
- Не задан формат вывода list (сценарий: тест ожидает "1. [ ] task", реализация — "1 task")
- Не определена нумерация с нуля или с одного (done 1 → не та задача при 0-индексах)
- Не описано поведение done <n> для несуществующего/некорректного номера
- Не описано поведение add при пустом тексте
- Не указано как list показывает выполненные задачи (R3 не тестируем)

#### Minor (Nice to Have)
- Коды выхода и формат ошибок не определены
- Поведение list при пустом списке не указано

### Оценка
Блокирует ли прохождение гейта: Да
```

**Наблюдения:**
- Все Important-находки содержат конкретный сценарий отказа — контракт соблюдён ✓
- Классификация Critical vs Important корректна ✓
- Оценка гейта вычислена механически по правилу (есть Critical → Да) ✓
- Формат вывода соответствует шаблону `spec-review.md` ✓

**Урок из прогона (characterization):**
`codex exec` зависает при передаче длинного промпта через `$(cat << 'HEREDOC')`.
Правильный рецепт: записать промпт в `/tmp/review-prompt.txt`, передать через stdin redirect.
Исправлено в `spawn-recipes.md`.

---

## Отклонения и follow-ups

1. **DeepSeek headless** — не проверено, API-ключ не настроен. Follow-up описан в `fallback-mode.md`.
2. **`~/.agents/skills/superpowers/`** — не резолвится локально. Инструкция добавлена в `fallback-mode.md` и README.
3. **Сквозной прогон полного конвейера** (диалог → писатель → ревью → план → реализация) — выходит за рамки этой сессии. Текст скилла проверен на контрактную корректность; полный E2E-прогон — в первой реальной сессии с роадмапом.
